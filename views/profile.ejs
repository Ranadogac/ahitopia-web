<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profilim | AhiTopia</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&family=Audiowide&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/web.css">
    <style>
        .profile-container { max-width: 1000px; margin: 120px auto 50px; padding: 20px; display: grid; grid-template-columns: 300px 1fr; gap: 30px; }
        .profile-card { background: rgba(255, 255, 255, 0.03); border: 1px solid rgba(255, 255, 255, 0.1); border-radius: 15px; padding: 30px; text-align: center; backdrop-filter: blur(10px); }
        .avatar-circle { width: 100px; height: 100px; background: linear-gradient(45deg, var(--primary), var(--secondary)); border-radius: 50%; margin: 0 auto 15px; display: flex; align-items: center; justify-content: center; font-size: 2.5rem; color: white; font-family: 'Audiowide', cursive; }
        .role-badge { background: rgba(255,255,255,0.1); padding: 5px 15px; border-radius: 20px; font-size: 0.8rem; color: var(--accent); display: inline-block; margin-top: 10px; }
        .profile-content { background: rgba(20, 20, 30, 0.8); border: 1px solid rgba(255, 255, 255, 0.05); border-radius: 15px; padding: 40px; }
        .form-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; margin-top: 20px; }
        .profile-input-group { margin-bottom: 15px; }
        .profile-input-group label { display: block; color: #888; margin-bottom: 8px; font-size: 0.9rem; }
        .profile-input-group input { width: 100%; background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.1); padding: 12px; border-radius: 8px; color: white; outline: none; transition: 0.3s; }
        .profile-input-group input:focus { border-color: var(--primary); box-shadow: 0 0 10px rgba(138, 43, 226, 0.2); }
        .balance-box { background: linear-gradient(to right, #1a1a2e, #16213e); border-left: 4px solid var(--accent); padding: 20px; margin-bottom: 30px; border-radius: 8px; display: flex; justify-content: space-between; align-items: center; }
        @media (max-width: 768px) { .profile-container { grid-template-columns: 1fr; margin-top: 100px; } }
    </style>
</head>
<body>
    <header id="navbar" style="background: rgba(10,10,10,0.9);">
        <div class="container nav-container">
            <a href="/" class="logo">AHI<span class="neon-text">TOPIA</span></a>
            <a href="/" class="btn btn-outline"><i class="fa-solid fa-arrow-left"></i> Ana Sayfaya Dön</a>
        </div>
    </header>

    <% 
        // 1. İsim Baş Harfi
        let initial = "U";
        if (user.name && user.name.length > 0) {
            initial = user.name.charAt(0).toUpperCase();
        } else if (user.email) {
            initial = user.email.charAt(0).toUpperCase();
        }

        // 2. Rol
        let roleName = (user.role === 'organizer') ? 'Organizatör' : 'Kullanıcı';
        let roleIcon = (user.role === 'organizer') ? 'fa-crown' : 'fa-user';
    %>

    <div class="profile-container">
        <div class="profile-card">
            <div class="avatar-circle">
                <%= initial %>
            </div>
            <h3><%= user.name || 'İsimsiz Kullanıcı' %></h3>
            <p style="color:#aaa; font-size:0.9rem;"><%= user.email %></p>
            
            <span class="role-badge">
                <i class="fa-solid <%= roleIcon %>"></i> <%= roleName %>
            </span>

            <div style="margin-top: 30px; text-align: left; padding-top: 20px; border-top: 1px solid rgba(255,255,255,0.1);">
                <p style="color:#888; font-size:0.9rem; margin-bottom:10px;">
                    <i class="fa-solid fa-check-circle" style="color: <%= user.is_verified ? '#00ff88' : '#ff4444' %>;"></i> 
                    E-posta: <%= user.is_verified ? 'Doğrulanmış' : 'Doğrulanmamış' %>
                </p>
                
                <% if(user.role === 'organizer') { %>
                    <p style="color:#888; font-size:0.9rem;">
                        <i class="fa-solid fa-shield-halved" style="color: <%= user.is_organizer_approved ? '#00ff88' : 'orange' %>;"></i> 
                        Yetki: <%= user.is_organizer_approved ? 'Onaylı' : 'İnceleniyor' %>
                    </p>
                <% } %>
            </div>
        </div>

        <div class="profile-content">
            <h2 class="title-decoration" style="margin-bottom: 20px;">Hesap Ayarları</h2>

            <% if(user.role === 'organizer') { %>
                <div class="balance-box">
                    <div>
                        <small style="color:#aaa;">Toplam Kazanç (Cüzdan)</small>
                        <h2 style="margin:0; color:var(--accent);"><%= user.balance || 0 %> ₺</h2>
                    </div>
                    <i class="fa-solid fa-wallet" style="font-size:2rem; color:#aaa;"></i>
                </div>
            <% } %>

            <form action="/update-profile" method="POST">
                <div class="form-grid">
                    <div class="profile-input-group">
                        <label>Ad</label>
                        <input type="text" name="name" value="<%= user.name %>" placeholder="Adınız">
                    </div>
                    <div class="profile-input-group">
                        <label>Soyad</label>
                        <input type="text" name="surname" value="<%= user.surname %>" placeholder="Soyadınız">
                    </div>
                </div>

                <div class="form-grid">
                    <div class="profile-input-group">
                        <label>E-posta (Değiştirilemez)</label>
                        <input type="email" value="<%= user.email %>" disabled style="opacity:0.5; cursor:not-allowed;">
                    </div>
                    <div class="profile-input-group">
                        <label>Telefon Numarası</label>
                        <input type="tel" name="phone" value="<%= user.phone %>" placeholder="05XX XXX XX XX">
                    </div>
                    <div class="profile-input-group">
                        <label>Adres (Ödeme alabilmek için zorunlu)</label>
                        <input type="text" name="address" value="<%= user.address || '' %>" placeholder="Mahalle, Sokak, İlçe, İl">
                    </div>
                </div>

                <% if(user.role === 'organizer') { %>
                    <div class="profile-input-group">
                        <label>IBAN (Ödemeleriniz için)</label>
                        <input type="text" name="iban" value="<%= user.iban %>" placeholder="TR00 0000...">
                    </div>
                <% } %>

                <button type="submit" class="btn btn-primary btn-glow" style="margin-top: 10px;">Bilgileri Güncelle</button>
            </form>
        </div>
    </div>
</body>
</html>